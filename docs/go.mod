// docs/ holds no Go code. This file makes it a module of its own, which keeps
// the docs site's recordings and screenshots out of the module zip that
// `go install github.com/gburgyan/aat/cmd/aat@version` downloads.
module github.com/gburgyan/aat/docs

go 1.25
