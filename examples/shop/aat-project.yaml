name: shop
description: Offline e-commerce sandbox (aat-sandbox), AAT's primary quick start
tags: [quickstart, e-commerce, sandbox, offline]
graph: graph.yaml
templates: templates/
domain: domain.yaml
workflows: workflows/
layers: layers/
# plans/ holds the reference plans the integration kit ships (see aat-kit.yaml);
# internal/plans/ holds the suites that stay here: the negative tests, resilience,
# and giftcard-express, which applies a layer.
plans: [plans/, internal/plans/]
visualizers: visualizers/
oas: openapi.yaml
environment: env.yaml
defaultEnvironment: us
archives: _output/runs
traces: _output/traces
